version: '3.8'
services:
  app:
    build: ./backend
    container_name: new-year-exchange
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - TZ=Asia/Shanghai

